@model IEnumerable<VentasApp.Models.EventoCategoria>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoriaEntrada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Evento)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoriaEntrada.CategoriaEntradaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Evento.EventoId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EventoCategoriaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EventoCategoriaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EventoCategoriaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
